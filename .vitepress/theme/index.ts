// xDBML site theme — extends VitePress default theme
// Adds custom CSS for brand colors and typography.

import DefaultTheme from 'vitepress/theme'
import './style.css'

export default DefaultTheme
