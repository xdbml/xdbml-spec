{
  "name": "@xdbml/render-api",
  "version": "0.1.0-poc.1",
  "description": "Stateless HTTP service that renders xDBML source to SVG. Runs as a Cloudflare Worker.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@xdbml/render": "^0.1.0-poc.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260617.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.102.0"
  }
}
