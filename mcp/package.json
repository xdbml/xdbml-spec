{
  "name": "@xdbml/mcp",
  "version": "0.2.0",
  "description": "Remote MCP server exposing the xDBML render core as a tool. Cloudflare Worker.",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:reference": "node scripts/generate-reference.mjs",
    "check:reference": "node scripts/generate-reference.mjs --check",
    "predev": "npm run generate:reference",
    "dev": "wrangler dev",
    "predeploy": "npm run generate:reference",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@resvg/resvg-wasm": "^2.6.2",
    "@xdbml/parse": "^0.3.0",
    "@xdbml/render": "^0.3.0",
    "agents": "^0.16.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260619.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.103.0"
  }
}
