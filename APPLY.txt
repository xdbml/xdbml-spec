xDBML -- v0.5 Delta 7a: examples for 12.4.1 (several levels) and 12.4.2 (several axes)
Base: Deltas 1-7 applied and committed locally on top of origin/main fb985b1.

WHY
---
Each snippet showed a single group, so neither illustrated its heading:
12.4.1 needs two stacked groups, 12.4.2 needs two groups on one supertype.
Their playground buttons opened the same single group, with stub entities.

WHAT CHANGED (spec\v0.5.md only)
--------------------------------
12.4.1  Complete example: Party, Person, Organization, Employee, Contractor,
        with legal_nature (Party -> Person, Organization) and person_role
        (Person -> Employee, Contractor). The paragraph after it now names
        both groups instead of pointing back to the chapter's first example.
12.4.2  Complete example: Party, Person, Organization, Customer, Supplier,
        with legal_nature (total, disjoint) and business_role (partial,
        overlapping) on the same supertype. The paragraph after it reads the
        two axes back.
Buttons Both snippets are now complete documents, so both buttons open the
        snippet exactly as written, with no added declarations. Regenerated
        with scripts\spec-playground-links.mjs --write.

A preview of both diagrams, rendered from the button links, is
v0.5-12.4-examples.png next to this package.

FILES
-----
  spec\v0.5.md    EDIT

APPLY (cmd.exe -- set REPO to your working clone)
-------------------------------------------------
set REPO=C:\path\to\xdbml-spec
set DELTA=%CD%

copy /Y "%DELTA%\spec\v0.5.md"   "%REPO%\spec\v0.5.md"

VERIFY
------
  cd /d %REPO%
  npm run check
  npm run docs:dev
  Open http://localhost:5173/spec/current#_12-4-1-several-levels and
  #_12-4-2-several-axes. To try a button locally, open it, then change
  https://xdbml.org to http://localhost:5173 in the address bar.

  Then commit WITHOUT pushing:

  git add spec\v0.5.md
  git commit -m "spec v0.5: examples for several levels and several axes"

VERIFICATION RUN HERE (Linux, Node 22)
---------------------------------------
  npm run check         exit 0; spec links: 113 buttons open their snippets
  both examples         no diagnostics; each button opens its snippet
                        verbatim; rendered: 12.4.1 shows Party > Person >
                        Employee/Contractor, 12.4.2 shows two symbols on
                        Party's bottom edge
