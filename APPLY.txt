xDBML -- v0.5 Delta 9a: roles, verbs and constraint type in the help; help in the Learn menus
Base: Deltas 1-9 applied and committed locally on top of origin/main fb985b1.

WHY
---
1. The relationships help page named roles, verbs and the constraint type
   only in passing. It now explains them.
2. The playground help was reachable only from the playground's Help
   button; no site page or menu linked it.

WHAT CHANGED
------------
playground\help\relationships-crows-foot.md
  New section "Roles, verbs and constraint type", after Conceptual
  relationships:
    Roles and verbs    two reading directions, a role per end and a verb
                       per direction, the four settings in one example,
                       which end each verb reads from, one/both/neither,
                       the inspector's Reading section, documentation only
    Identifying and    what each means (order line vs order), both written
    non-identifying    out, intent only (adds and checks no key), absent
                       means unstated rather than non-identifying, foreign
                       keys only, drawn the same way, shown as Constraint
                       in the inspector
.vitepress\config.ts
  "Playground help" -> /playground/help/getting-started, after Examples,
  in the top-level Learn menu and in the four "Learn xDBML" sidebar
  groups.
CHANGELOG.md
  Playground help entry mentions the new section; Changed > Tooling gains
  the Learn-menu link.

FILES
-----
  playground\help\relationships-crows-foot.md   EDIT
  .vitepress\config.ts                          EDIT
  CHANGELOG.md                                  EDIT

APPLY (cmd.exe -- set REPO to your working clone)
-------------------------------------------------
set REPO=C:\path\to\xdbml-spec
set DELTA=%CD%

copy /Y "%DELTA%\playground\help\relationships-crows-foot.md"  "%REPO%\playground\help\relationships-crows-foot.md"
copy /Y "%DELTA%\.vitepress\config.ts"                         "%REPO%\.vitepress\config.ts"
copy /Y "%DELTA%\CHANGELOG.md"                                 "%REPO%\CHANGELOG.md"

VERIFY
------
  cd /d %REPO%
  npm run check
  npm run docs:dev
  - Learn menu (top bar): "Playground help" opens Getting started, with the
    help sidebar
  - http://localhost:5173/playground/help/relationships-crows-foot, section
    "Roles, verbs and constraint type"

  Then commit WITHOUT pushing:

  git add -A
  git commit -m "Help: roles, verbs and constraint type; playground help in the Learn menus"

VERIFICATION RUN HERE (Linux, Node 22)
---------------------------------------
  npm run check    exit 0, docs build with dead-link check; the built FAQ
                   page links /playground/help/getting-started
  claims checked   the parser reports constraint_type on a foreign master
                   (constraint-type-on-foreign-master) but not on `<>`, so
                   the page says only the former is reported
